// Erlli Web/src/helpers/common.js
export const hp = (percentage) => {
  return `${percentage}vh`; // Convert percentage to viewport height units
};

export const wp = (percentage) => {
  return `${percentage}vw`; // Convert percentage to viewport width units
}; 
